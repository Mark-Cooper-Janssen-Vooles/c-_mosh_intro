using System;
//because project is named 28_classes, every time i create a new class its added by default to this namespace.

    //we wanna group related classes into different namespaces.

namespace _28_classes.Math
{
    public class Calculator
    {
       public int Add(int a, int b)
        {
            return a + b;
        }
    }
}
